# Copy to backend/.env and set values. Never commit .env.

# Required for Mapbox geocoding and directions
MAPBOX_ACCESS_TOKEN=your_mapbox_access_token

# Production: set a strong random key (e.g. openssl rand -base64 48)
# DJANGO_SECRET_KEY=

# Production: set to false
# DJANGO_DEBUG=false

# Production: comma-separated list of allowed hosts
# DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Production: comma-separated CORS origins (your frontend URL)
# DJANGO_CORS_ORIGINS=https://your-frontend.vercel.app
