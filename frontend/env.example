# Copy to frontend/.env and set values. Never commit .env.
# Vite only exposes variables prefixed with VITE_.

# API base URL (no trailing slash). Default for local dev: http://localhost:8000
# VITE_API_URL=http://localhost:8000

# Required for the map. Get a token at https://account.mapbox.com/
# VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_public_token
